Your script MUST define and call this output function. The pipeline executes the script and captures the return value.

def get_geometric_output():
    """Return the structure as JSON-serializable dict for GH import. 2D [x, z] plane; y=0 in Rhino."""
    return {
        "description": "2D truss in [x, z] plane. Units: m, kN. Supports pinned.",
        "line_elements": [
            {"start": [x1, z1], "end": [x2, z2]},
            # ... all lines: top chord, bottom chord, diagonals
        ],
        "support": [
            [x, z],
            # ... support points
        ],
        "loads": [
            [x, z, Fz],
            # ... one per load: application point (x,z) and vertical force Fz in kN
        ],
    }

# At end of script:
if __name__ == "__main__":
    out = get_geometric_output()
    import json
    print(json.dumps(out))
