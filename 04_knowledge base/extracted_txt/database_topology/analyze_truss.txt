2. analyze_trusspythondef analyze_truss(nodes, elements, areas, fixed_dofs, external_loads): """Performs Finite Element Analysis (FEA) including self-weight. Returns: tuple: (displacements, strains, stresses, element_lengths) or (None, None, None, None) on failure """ num_nodes, num_elements, num_dofs = len(nodes), len(elements), len(nodes) * 2 K = np.zeros((num_dofs, num_dofs)) element_lengths = np.zeros(num_elements) self_weight_forces = np.zeros((num_nodes, 2)) for i, (n1_idx, n2_idx) in enumerate(elements): L = np.linalg.norm(nodes[n2_idx] - nodes[n1_idx]) element_lengths[i] = L member_weight = areas[i] * L * STEEL_DENSITY * 9.81 / 1000.0 self_weight_forces[n1_idx, 1] -= member_weight / 2.0 self_weight_forces[n2_idx, 1] -= member_weight / 2.0 if L == 0: continue c, s = (nodes[n2_idx,0] - nodes[n1_idx,0]) / L, (nodes[n2_idx,1] - nodes[n1_idx,1]) / L k_e = (areas[i] * STEEL_E / L) * np.array([[c*c, c*s, -c*c, -c*s], [c*s, s*s, -c*s, -s*s], [-c*c, -c*s, c*c, c*s], [-c*s, -s*s, c*s, s*s]]) dofs = [2*n1_idx, 2*n1_idx+1, 2*n2_idx, 2*n2_idx+1] K[np.ix_(dofs, dofs)] += k_e F = external_loads.flatten() + self_weight_forces.flatten() free_dofs = np.setdiff1d(range(num_dofs), fixed_dofs) U = np.zeros(num_dofs) try: U[free_dofs] = np.linalg.solve(K[np.ix_(free_dofs, free_dofs)], F[free_dofs]) except np.linalg.LinAlgError: return None, None, None, None strains = np.zeros(num_elements) for i, (n1_idx, n2_idx) in enumerate(elements): L = element_lengths[i] if L == 0: continue c, s = (nodes[n2_idx,0] - nodes[n1_idx,0])/L, (nodes[n2_idx,1] - nodes[n1_idx,1])/L strains[i] = (c * (U[2*n2_idx] - U[2*n1_idx]) + s * (U[2*n2_idx+1] - U[2*n1_idx+1])) / L stresses = STEEL_E * strains return U.reshape((num_nodes, 2)), strains, stresses, element_lengthsMetadata:Dependencies: numpy, Global constants: STEEL_E, STEEL_DENSITYInputs:nodes: Node coordinateselements: Member connectionsareas: Cross-sectional areas of membersfixed_dofs: Constrained degrees of freedomexternal_loads: Applied nodal loadsOutputs: Displacements, strains, stresses, and member lengthsDescription:Performs linear static FEA by:Building global stiffness matrixCalculating self-weight forcesSolving displacement equationsComputing member strains and stressesHandling singular matrix errors (unstable structures)