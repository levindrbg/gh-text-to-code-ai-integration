3. calculate_buckling_stresspythondef calculate_buckling_stress(areas, lengths): """Calculates critical buckling stress using Euler's formula. Returns: np.array: Allowable compressive stresses for each member """ safe_areas = np.maximum(areas, MIN_AREA) I = safe_areas**2 / (8 * np.pi * T_D_RATIO) effective_lengths = K_FACTOR_BUCKLING * lengths + 1e-9 critical_stress = (np.pi**2 * STEEL_E * I) / (effective_lengths**2 * safe_areas) return np.minimum(critical_stress, STEEL_STRENGTH_COMPRESSION)Metadata:Dependencies: numpy, math, Globals: MIN_AREA, T_D_RATIO, K_FACTOR_BUCKLING, STEEL_E, STEEL_STRENGTH_COMPRESSIONInputs:areas: Member cross-sectional areaslengths: Member lengthsOutput: Array of allowable compressive stressesDescription:Computes buckling resistance for compression members by:Approximating moment of inertia from areaCalculating Euler buckling stressCapping stress at material compressive strengthAdding epsilon to avoid division by zero