1. setup_truss_ground_structurepythondef setup_truss_ground_structure(): """ Creates the initial dense mesh of nodes and potential elements (ground structure). Returns: tuple: (nodes, elements, fixed_dofs, load_nodes, load_values, chord_indices, web_indices) """ nodes = [] for i in range(NUM_BAYS + 1): x = i * (SPAN / NUM_BAYS) nodes.append([x, 0]) nodes.append([x, HEIGHT]) nodes = np.array(nodes) elements, chord_indices, web_indices = [], [], [] element_counter = 0 for i in range(len(nodes)): for j in range(i + 1, len(nodes)): node1_x, node1_y = nodes[i] node2_x, node2_y = nodes[j] if abs(node1_x - node2_x) <= 3 * (SPAN / NUM_BAYS): elements.append([i, j]) is_chord = (node1_y == node2_y) and (abs(node1_x - node2_x) <= (SPAN / NUM_BAYS) * 1.01) if is_chord: chord_indices.append(element_counter) else: web_indices.append(element_counter) element_counter += 1 elements = np.array(elements) bottom_right_node_idx = 2 * NUM_BAYS fixed_dofs = [0, 1, 2 * bottom_right_node_idx + 1] load_nodes, load_values = [], [] bay_width = SPAN / NUM_BAYS for i in range(1, len(nodes), 2): load_nodes.append(i) load = -FACTORED_LINE_LOAD_PER_MM * bay_width if i == 1 or i == (len(nodes) - 1): load /= 2 load_values.append([0, load]) return nodes, elements, fixed_dofs, np.array(load_nodes), np.array(load_values), np.array(chord_indices), np.array(web_indices)Metadata:Dependencies: numpy (as np), Global constants: SPAN, HEIGHT, NUM_BAYS, FACTORED_LINE_LOAD_PER_MMInputs: NoneOutputs:nodes: Array of node coordinates [x,y] in mmelements: Array of member connections [node_i, node_j]fixed_dofs: List of constrained degrees of freedomload_nodes: Indices of loaded nodesload_values: Corresponding load vectors [Fx, Fy]chord_indices: Indices of chord membersweb_indices: Indices of web membersDescription:Generates the initial truss topology by:Creating nodes along top and bottom chordsConnecting nodes within 3-bay proximityClassifying members as chords (horizontal) or webs (diagonal/vertical)Applying boundary conditions (left fixed, right roller)Calculating nodal loads from distributed load