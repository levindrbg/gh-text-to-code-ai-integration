4. update_areas_beso_with_chordspythondef update_areas_beso_with_chords(areas, stresses, element_lengths, chord_indices, web_indices): """Updates areas using BESO method with special chord handling. Returns: np.array: Updated member areas """ allowable_compressive_stress = calculate_buckling_stress(areas, element_lengths) allowable_stresses = np.where(stresses >= 0, STEEL_STRENGTH_TENSION, allowable_compressive_stress) allowable_stresses[allowable_stresses < 1e-3] = 1.0 stress_ratio = np.abs(stresses) / allowable_stresses new_areas = (1 - MAX_STRESS_ADJUSTMENT_RATE) * areas + MAX_STRESS_ADJUSTMENT_RATE * (areas * stress_ratio) web_areas = new_areas[web_indices] web_stresses = stresses[web_indices] web_lengths = element_lengths[web_indices] target_web_volume = np.sum(web_areas * web_lengths) * (1 - EVOLUTIONARY_RATE) sensitivity = np.abs(web_stresses) sorted_web_indices_by_sensitivity = web_indices[np.argsort(sensitivity)] current_web_volume = np.sum(new_areas[web_indices] * web_lengths) for idx in sorted_web_indices_by_sensitivity: if current_web_volume <= target_web_volume: break volume_of_element = (new_areas[idx] - MIN_AREA) * element_lengths[idx] if volume_of_element > 0: new_areas[idx] = MIN_AREA current_web_volume -= volume_of_element return np.maximum(MIN_AREA, new_areas)Metadata:Dependencies: numpy, calculate_buckling_stress, Globals: STEEL_STRENGTH_TENSION, MIN_AREA, EVOLUTIONARY_RATE, MAX_STRESS_ADJUSTMENT_RATEInputs:areas: Current member areasstresses: Calculated member stresseselement_lengths: Member lengthschord_indices: Indices of chord membersweb_indices: Indices of web membersOutput: Updated member areas after optimizationDescription:Implements BESO optimization by:Calculating stress utilization ratiosAdjusting areas based on stress levelsApplying evolutionary volume reduction to web membersRemoving low-stress web members firstEnforcing minimum area constraints