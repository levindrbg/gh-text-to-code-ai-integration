# --- MAIN RUNNER ---if __name__ == "__main__": nodes, elements, fixed_dofs, load_nodes, load_values = setup_truss_ground_structure() num_elements = len(elements) areas = np.full(num_elements, INITIAL_AREA) materials = [{'type': 'steel', 'E': STEEL_E, 'strength': STEEL_STRENGTH}] * num_elements for i in range(NUM_ITERATIONS): result = analyze_truss(nodes, elements, areas, materials, fixed_dofs, load_nodes, load_values) if result[0] is None: break displacements, strains, stresses, lengths = result for j in range(num_elements): if stresses[j] >= 0: materials[j] = {'type': 'steel', 'E': STEEL_E, 'strength': STEEL_STRENGTH} else: materials[j] = {'type': 'cfrp', 'E': CFRP_E, 'strength': CFRP_STRENGTH} target_areas = np.abs(stresses) / np.array([m['strength'] for m in materials]) * areas temperature = 1.0 - (i / NUM_ITERATIONS) random_factor = 1.0 + (np.random.rand(num_elements) - 0.5) * 0.1 * temperature new_areas = target_areas * random_factor if i > COMPLEXITY_CONTROL_START_ITER: max_area = np.max(areas) for j in range(num_elements): if areas[j] < 0.05 * max_area and np.abs(stresses[j]) < 0.5 * materials[j]['strength']: new_areas[j] *= 0.7 areas = np.clip(new_areas, MIN_AREA, None) final = analyze_truss(nodes, elements, areas, materials, fixed_dofs, load_nodes, load_values) if final[0] is not None: export_for_karamba(nodes, elements, areas, materials)